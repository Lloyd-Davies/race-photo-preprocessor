"""race-photo-store admin API client. (Phase 6+)"""
from __future__ import annotations

# Placeholder — Phase 6+ will implement:
#   - list_events(base_url, token) -> list[dict]
#   - trigger_ingest(base_url, token, event_id) -> dict
#   - upload_bib_tags(base_url, token, event_id, tags) -> dict
#
# Uses httpx for sync HTTP calls (called from QThread workers, not main thread).
