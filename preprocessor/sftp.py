"""SFTP helpers using Paramiko. (Phase 7)"""
from __future__ import annotations

# Placeholder — Phase 7 will implement:
#   - connect(host, port, username, key_path) -> SFTPClient
#   - upload_file(sftp, local_path, remote_path, progress_cb)
#   - test_connection(host, port, username, key_path) -> bool, str
#   - sync_tree(sftp, local_root, remote_root, skip_existing, progress_cb)
